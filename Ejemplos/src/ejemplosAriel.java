

import java.util.Scanner;

public class ejemplosAriel {
	//main + control espacio y despues enter
	// para pasar palabra por palabra control + flechas
	// para comentar una linea control + 7
	//	para borrar algo con control + shitf + flechas 
	//	para borrar una linea control + D
	//	para copia todo lo que esta en una linea control + alt + flecha para abajo
	//	para aumentar o disminuir las letras control + shif + "+" o "-"
	//	para buscar una linea control + l
	//  para mover una linea entera de lugar alt + flechas
	//	borrar todo de izquierda a derecha con control shift Supr
	//	
	 static Scanner sc = new Scanner(System.in);

	public static void main(String[] args) {	
		
		
				/*int TIEMPO_MIN = 0;
				int TIEMPO_MAX = 100;
				 int dia = 1;
				 int dias = 10;
				 boolean esApto = true;
				 int contadorTiempo = 0;
				 boolean aptoPrueba15Minutos = false;
				 while( dia <= dias && esApto == true) {
					 System.out.println("Dia:  " + dia);
					 int tiempo;
					do { System.out.println("Ingresar el tiempo de prueba en minutos: ");
						   tiempo = sc.nextInt();
						 
						 if(tiempo < 0 || tiempo > 100) {
							 System.out.println("Error: el tiempo ingresado es invalido.");
						 }
					 }while(tiempo < 0 || tiempo > 100);
					 if(tiempo > 20) {
						 esApto = false;}
					 if (tiempo < 15) {
						 aptoPrueba15Minutos = true;}
					 contadorTiempo += tiempo;}
				 float promedio = contadorTiempo / dias;
				 if(esApto == true && aptoPrueba15Minutos== true && promedio < 18 ) {
					 System.out.println("EL jugador es apto");
				 }else {System.out.println("El jugador no es apto.");}*/
			}

}
